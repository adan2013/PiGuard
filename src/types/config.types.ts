export interface GpioPins {
  trigger1: number;
  trigger2: number;
  trigger3: number;
}

export interface TriggerNames {
  trigger1: string;
  trigger2: string;
  trigger3: string;
}

export interface FrontPanelGpioPins {
  led: number;
  speaker: number;
  switch1: number;
  switch2: number;
}
